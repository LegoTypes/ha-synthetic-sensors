# Focused YAML fixture for debugging boolean variable issue
# Just the failing boolean variable conditional test

version: "1.0"

global_settings:
  device_identifier: "test_device_123"

sensors:
  boolean_variable_conditional:
    name: "Boolean Variable Conditional Test"
    formula: "state"
    variables:
      door_status: "binary_sensor.front_door"
    attributes:
      debug_door_status: door_status
      debug_conditional_result:
        formula: "'closed' if not door_status else 'open'"
        alternate_states:
          FALLBACK: unknown
    metadata:
      device_class: "door"