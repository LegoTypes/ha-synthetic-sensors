# Test YAML file with 'if' as a variable name
# This should fail validation - 'if' is a reserved word

sensors:
  test_sensor:
    name: "Test Sensor with Reserved Word 'if'"
    formula: "value + 10"
    variables:
      value: "sensor.source_entity"
      if: "sensor.condition_entity"  # Reserved word - should fail
