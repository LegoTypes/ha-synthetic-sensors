version: "1.0"

global_settings:
  device_identifier: "string_test_device"

sensors:
  string_concatenation_sensor:
    name: "String Concatenation Test"
    formula: "'Device: ' + 'test_string'"
    metadata:
      unit_of_measurement: ""
      device_class: "enum"
      state_class: "measurement"

  mixed_string_variable_sensor:
    name: "Mixed String Variable Test"
    formula: "'Status: ' + state + ' active'"
    variables:
      state: "sensor.test_sensor"
    metadata:
      unit_of_measurement: ""
      device_class: "enum"

  numeric_default_sensor:
    name: "Numeric Default Test"
    formula: "state * 1.1"
    variables:
      state: "sensor.power_meter"
    metadata:
      unit_of_measurement: "W"
      device_class: "power"
      state_class: "measurement"