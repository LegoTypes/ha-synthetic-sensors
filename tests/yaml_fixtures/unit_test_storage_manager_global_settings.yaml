# YAML fixture for test_storage_manager.py storage tests
# Used by: test_async_update_sensor_with_global_settings
# Test cases covered:
# - Global settings with device_identifier and variables
# - Global settings inheritance and application

version: "1.0"

global_settings:
  device_identifier: "global_device:test_123"
  variables:
    global_var: "sensor.global_entity"

sensors:
  test_sensor:
    name: "Test Sensor"
    formula: "1 + 2"