# YAML fixture for test_yaml_operations.py
# Used by: test_load_config_success
# Test cases covered:
# - Successful config loading
# - Valid sensor configuration with variables

version: "1.0"
sensors:
  test_sensor:
    name: "Test Sensor"
    formula: "A + B"
    variables:
      A: "sensor.test_a"
      B: "sensor.test_b"